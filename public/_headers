# The font filename is stable (not content-hashed), so cache it hard.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# Vite hashes asset filenames, so they are safe to cache immutably too.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
